name: CI

on:
  push:
    branches: [ "master", "classic"]
  pull_request:
    branches: [ "master", "classic" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    #- name: configure
    #  run: ./configure
    - name: make
      run: gcc --version; cd vic && make
    #- name: make check
    #  run: make check
    #- name: make distcheck
    #  run: make distcheck
